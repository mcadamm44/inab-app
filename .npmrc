# NPM Registry Configuration
registry=https://registry.npmjs.org/

# Publishing configuration
access=public

# Authentication (uncomment and configure as needed)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Scope configuration (if using scoped packages)
# @your-scope:registry=https://registry.npmjs.org/

# Additional registry configurations
# For GitHub Packages (uncomment if needed)
# @your-org:registry=https://npm.pkg.github.com/
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# For private registries (uncomment and configure as needed)
# @your-private-scope:registry=https://your-private-registry.com/
# //your-private-registry.com/:_authToken=${PRIVATE_TOKEN} 